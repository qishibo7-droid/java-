# 学习记录 2026-09-11

**学习阶段**：第一阶段 · 基础地基期（2026.09 — 2027.02）
**今日主题**：Java 基础语法结构 · 基本数据类型与取值范围 · 与 Python 的迁移对比 · 二进制补码
**产出文件**：`src/learningdata1.java`

---

## 一、今日学习内容

### 1. Java 基础语法结构

- 程序入口固定写法 `public static void main(String[] args)`
- 类的定义：`public class 类名 { ... }`
- 三种注释：单行 `//`、多行 `/* */`、文档注释 `/** */`
- 输出：`System.out.println(...)`（`System` 是类，`out` 是对象，`println` 是方法）

### 2. 变量与基本数据类型

| 类型 | 占用位数 | 取值范围 | 易错点 |
|---|---|---|---|
| `byte` | 8 | -128 ~ 127 | 超范围赋值直接编译报错 |
| `short` | 16 | -32768 ~ 32767 | |
| `int` | 32 | -2³¹ ~ 2³¹-1 | 整数字面量**默认**是 `int` |
| `long` | 64 | -2⁶³ ~ 2⁶³-1 | 字面量必须加 `L` 后缀，否则超 `int` 范围就报错 |

### 3. 常量与类型转换

- `final` 修饰常量，赋值后不可修改（被注释掉的 `a1 = a1 + 1;` 就是在验证这一点）
- 隐式（自动）转换：**小范围 → 大范围**才自动完成，如 `byte b = a;`（`byte` → `short`）
- 反向转换（大范围 → 小范围）必须显式强制转换，否则编译报错（`a = b;` 报错即为此）

### 4. 与 Python 的知识迁移对比

| 对比项 | Python | Java |
|---|---|---|
| 类型系统 | 动态类型，`a = 1` 直接推断 | 静态强类型，`int a = 1;` 必须声明 |
| 整数精度 | **任意精度**，自动扩容不溢出 | 定长位数，溢出后**静默回绕**（不报错） |
| 位宽意识 | 基本可以不关心 | 必须时刻清楚，是理解补码/溢出的前提 |

> 这个差异是今天最重要的收获：Python 里 `2**100` 直接算得出来，Java 里就必须先想清楚"装不装得下"。

### 5. 二进制与米奇试药问题

「米奇试药」即经典的小白鼠试毒问题：*n* 只米奇能一次性定位出 2ⁿ 瓶药中的毒药——每只米奇代表二进制的一位，喂药方案就是毒药编号的二进制表示。1000 瓶需要 10 只（2¹⁰ = 1024）。

对应到 Java 的知识点是：**位模式既是数值，也是编码**。这为后面学位运算符（`&`、`|`、`^`、`~`、`<<`、`>>`、`>>>`）打下了直觉基础。

---

## 二、代码自检与待修正项

代码已通过编译运行验证（`javac` 无警告，输出 `127`），语法与逻辑正确。以下为笔记表述上的**精确化修正**，逐条修正后本节内容可直接用于面试：

### ① 数值范围写错了（两处）

```java
// 原注释：
// int  类型数据的范围是 2的32次方 到 2的32次方-1
// long 类型数据的范围是 2的64次方 到 2的64次方-1
```

**应为**：
- `int`：**-2³¹ ~ 2³¹-1**（不是 2³²）
- `long`：**-2⁶³ ~ 2⁶³-1**（不是 2⁶⁴）

规律：*n* 位有符号整数的范围是 **-2ⁿ⁻¹ ~ 2ⁿ⁻¹-1**。位数是 32 和 64，指数是 31 和 63。

### ② 「为什么负数不需要 -1」——方向对，表述要收紧

原注释里"补码是反码基础上 +1"这个理解是对的。更准确的表述是：

*n* 位一共产生 2ⁿ 个位模式，划分为：

- 非负：**2ⁿ⁻¹** 个（**包含 0**）→ 因为 0 占掉一个，最大正数只能是 2ⁿ⁻¹**-1**
- 负数：**2ⁿ⁻¹** 个 → 能一直取到 -2ⁿ⁻¹，**不需要减 1**

一句话：**减 1 是因为 0 占了一个坑，不是因为"二进制表达的限制"**。

> 待补充：非负其实是 2ⁿ⁻¹ 个，其中 0 占一个、正数只剩 2ⁿ⁻¹-1 个，所以负数比正数多一个（-128 vs 127）——这是补码"多表示一个负数"的由来。

### ③ 「隐式转化要求双方都有符号或无符号」——Java 里不适用

```java
// 原注释：
// 隐形转化的要求：1小范围转化到大范围，2要么双方都有符号，要么双方都无符号
```

**第 2 条是 C/C++ 的规则，不是 Java 的。** Java 的整数类型**全部有符号，没有 `unsigned`**，所以不存在"符号性匹配"的问题。

Java 隐式转换只需满足一条：**目标类型范围 ≥ 源类型范围**。

拓宽方向：`byte → short → int → long → float → double`

### ④ 类名命名规范（硬性规则）

- 文件名 `learningdata1.java` 与 public 类名一致，**这一条是对的**，编译能过
- 但 Java 约定：**类名用大驼峰 PascalCase**，应为 `LearningData1`
- 建议重命名为 `src/LearningData1.java` + `public class LearningData1`

### ⑤ `src/Main.java.bak` 应删除

该备份文件里定义了**两个 public 类**（`learningdata1` 和 `abc`）——一个 `.java` 文件里最多只能有一个 public 类，这段代码**无法编译**。留着的唯一价值是"记录自己改过什么"，而这个需求由 Git 本身承担。建议删除。

### ⑥ 小建议：用十六进制/二进制字面量

```java
byte a = 0x7F;          // 比 127 更直观地看出"低 7 位全是 1"
int  c = 0b0111_1111;   // 直接对应今天学的位模式
```

十进制的 `127` 看不出位模式，而今天刚学的正是二进制与补码——用进制字面量能把这个知识点接上。

---

## 三、下一步计划

- [ ] 按上面 ①~⑥ 修正 `learningdata1.java` 的注释，重命名类为 `LearningData1`
- [ ] 删除 `src/Main.java.bak`
- [ ] 补充其余基本类型：`float` / `double` / `char` / `boolean`
- [ ] 掌握算术、关系、逻辑、位运算符（把米奇试药的位运算亲手实现一遍）
- [ ] 掌握流程控制：`if` / `switch` / `for` / `while`
- [ ] 数组与方法

---

## 四、本阶段验收标准对照

出自《本科就业学习规划书》表 2：

| 验收项 | 当前进度 |
|---|---|
| Java SE 系统学习（面向对象、集合、异常、IO） | 🔄 刚开始，基础语法 |
| 注册 GitHub 并养成提交习惯 | 🔄 首次提交 |
| 产出：Java 小工具仓库 | ⬜ 未开始 |
| 牛客 SQL 初阶+中阶约 100 题 | ⬜ 未开始（计划 2026.11—12） |
| LeetCode 累计 80 题 | ⬜ 未开始（计划 2027.01—02） |

> 阶段跨度：2026.09 — 2027.02（每周约 15 小时）
