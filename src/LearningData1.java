/*2026年学习java第一天，注释练习

 */

public class LearningData1 {
    public static void main(String[] args) {
        /*System.out.println("hello world");//打印hello world
        System.out.println(123);
        int a=100,b=50;
        int c;
        c=a+b;
        System.out.println(c);
        final int a1=900;
        //a1=a1+1;
        System.out.println(a1);  */
        // 范围规律：n 位有符号整数 = -2的(n-1)次方 到 2的(n-1)次方-1
        // 注意指数是 位数减1，不是位数本身
        byte a=127; //byte数据范围是-128到127（即-2的7次方到2的7次方-1），因为是由8个比特组成
        short b=30000;//short类型数据范围是-2的15次方到2的15次方-1，因为是由16个比特组成
        int c=2000000000;//int类型数据的范围是-2的31次方到2的31次方-1，因为是由32个比特组成
        long d=2000000000000000000L;//long类型数据的范围是-2的63次方到2的63次方-1，因为是由64个比特组成
        /*为什么正数最大值要-1、负数最小值不用-1：
         n 位一共只有 2的n次方 个位模式。
         其中以0开头的 2的(n-1)次方 个表示非负，但 0 自己就占掉了一个，
         所以正数只剩下 2的(n-1)次方-1 个，最大值才会是 2的(n-1)次方-1；
         以1开头的 2的(n-1)次方 个全部表示负数，一个不浪费，最小能取到 -2的(n-1)次方。
         一句话：正数要-1 是因为 0 占了一个坑，不是因为二进制的表达方式有缺陷。
         这也是补码的好处——0只有唯一一种表示，省下来的那个位模式就多表示了一个负数，
         所以 byte 能取到 -128，正数最大却只有 127。
         */
        //a=b;//报错：short -> byte 属于“大范围 -> 小范围”，不允许隐式转换（编译期就拦住了，哪怕 b 的实际值是 30000 或 127 也一样）
        b=a;   //byte -> short 是“小范围 -> 大范围”，自动完成
        System.out.println(b);
        //隐形转化的要求：目标类型的范围 >= 源类型的范围，即只允许“小范围 -> 大范围”
        //拓宽方向：byte -> short -> int -> long -> float -> double
        //注意：Java 的整数类型全部有符号，没有 unsigned，所以不存在“符号性要一致”的要求
    }
}